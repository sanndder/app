{
  "remove": [
    "set",
    "password",
    "user_type",
    "user_id",
    "new_key",
    "reset_key",
    "timestamp",
    "deleted",
    "deleted_by",
    "deleted_on"
  ],
  "field": {
    "username": {
      "label": "Emailadres",
      "rules": {
        "required": true,
        "minlength": 3,
        "maxlength": 200,
        "removespaces": true,
        "type" : "email"
      }
    },
    "naam": {
      "label": "Naam gebruiker",
      "rules": {
        "required": true,
        "minlength": 2,
        "maxlength": 100
      }
    }
  }
}