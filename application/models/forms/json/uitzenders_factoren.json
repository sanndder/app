{
  "remove": [
    "set",
    "id",
    "user_id",
    "uitzender_id",
    "contact_id",
    "timestamp",
    "deleted",
    "deleted_by",
    "deleted_on"
  ],
  "field": {
    "factor_hoog": {
      "label": "Factor standaard uren",
      "rules": {
        "required": true,
        "minlength": 1,
        "maxlength": 5,
        "removespaces": true,
        "type": "factor"
      }
    },
    "factor_laag": {
      "label": "Factor overuren",
      "rules": {
        "required": true,
        "minlength": 1,
        "maxlength": 5,
        "removespaces": true,
        "type": "factor"
      }
    }
  }
}