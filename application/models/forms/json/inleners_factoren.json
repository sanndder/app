{
  "remove": [
    "set",
    "id",
    "user_id",
    "inlerner_id",
    "contact_id",
    "timestamp",
    "deleted",
    "deleted_by",
    "deleted_on"
  ],
  "field": {
    "factor_id": {
      "label": "Factor",
      "rules": {
        "required": true,
        "type": "int"
      }
    },
    "inlener_id": {
      "label": "Inlener ID",
      "rules": {
        "required": true,
        "type": "int"
      }
    },
    "omschrijving": {
      "label": "Omschrijving",
      "rules": {
        "required": true,
        "minlength": 1,
        "maxlength": 100,
        "striptags": true
      }
    },
    "factor_hoog": {
      "label": "Factor standaard uren",
      "rules": {
        "required": true,
        "minlength": 1,
        "maxlength": 5,
        "removespaces": true,
        "type": "factor"
      }
    },
    "factor_laag": {
      "label": "Factor overuren",
      "rules": {
        "required": true,
        "minlength": 1,
        "maxlength": 5,
        "removespaces": true,
        "type": "factor"
      }
    },
    "default_factor": {
      "label": "Standaard",
      "rules": {
        "required": true,
        "type": "int"
      }
    }
  }
}